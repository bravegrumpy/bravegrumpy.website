version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Starting build process"
    build:
      commands:
        - echo "Building Docker image"
        - docker build -t my-php-app .
    postBuild:
      commands:
        - echo "Build completed"
  artifacts:
    baseDirectory: src
    files:
      - '**/*'
  cache:
    paths: []