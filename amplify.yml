version: 1
applications:
  - appRoot: src
    type: docker
    build:
      commands:
        - docker build -t my-php-app .
    port: 80
    frontend:
      phases:
        build:
          commands: [""]
      artifacts: 
        baseDirectory: src
        files:
          - "**/*"
