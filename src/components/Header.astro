---
import Logo from './Logo.svelte';
import TopNav from './TopNav';
import ThemeSwitch from './ThemeSwitch.astro';

let size = "75px";
---
<header>
    <Logo client:load size={size}/> 
</header>

<style lang="less">
    @import '@styles/declarations.less';
    header {
        display: flex;
        flex-direction: row;
        align-items: flex-start;
        justify-content: space-between;
        border: 2px solid @base0;
        width: 100%;
        gap: 10px;
    }
    [data-icon] {
        color: @yellow;
        font-size: 3rem;
    }
</style>